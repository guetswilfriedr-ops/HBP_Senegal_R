# ============================================================
# HBP indicator calculations
# Add one function per indicator; keep each function focused
# on a single, well-named computation.
# ============================================================

library(dplyr)

#' Template: compute a coverage indicator
#'
#' Replace the body with the real calculation once the cleaned
#' data structure (column names, units) is confirmed.
#'
#' @param cleaned_data Named list of cleaned data frames
#' @return A data frame with the computed indicator
compute_coverage <- function(cleaned_data) {
  stop("compute_coverage() is a template: implement once the data structure is confirmed.")
}
